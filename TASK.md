我有一个网站项目，技术栈如下：
- 前端：Vite 8 + React 19 + TailwindCSS
- 后端：Express 5
- AI：DeepSeek V4（通过 Node.js 后端调用）
- 端口：3001（后端），5174（前端）

我想把这个网站部署到公网，让任何人都能访问，要求：
1. 免费或低成本方案（可以用 GitHub + Cloudflare Pages / Vercel / Render / Railway / Docker）。
2. 包含前端、后端、AI 接口的完整部署。
3. 提供每一步操作命令和说明。
4. 如果涉及 GitHub 仓库或 Docker 镜像，生成完整命令。
5. 给出 Windows 系统下的具体操作方法，并标注命令行输入步骤。
6. 注意安全性和环境变量管理，确保 DeepSeek API Key 不泄露。

请帮我生成一个完整的部署方案，我会逐步执行。